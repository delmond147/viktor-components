## How to run this project

1. Make sure you have Node.js installed.
2. clone the repo.
3. go to cta-sec folder.
4. run `npm install`.
5. run `npm start`.

The project will start at `http://localhost:3000`.
do the same for card-sec folder and any other folder.


## Tech Stack
- React
- Vite
- TypeScript

